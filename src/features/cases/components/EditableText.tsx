"use client";

import { useState } from "react";

type Props = {
    value: string;
    onSave: (value: string) => void;
    className?: string;
};

export const EditableText = ({
    value,
    onSave,
    className,
}: Props) => {
    
};